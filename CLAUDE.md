# Claude Code

This repository keeps its operational handbook in [`AGENTS.md`](./AGENTS.md), the
tool-agnostic location every coding agent reads. Nothing Claude-specific lives
here; this file exists so that `CLAUDE.md` discovery still resolves, and it
imports the handbook so the content is loaded either way.

Edit `AGENTS.md`, never this file.

@AGENTS.md
