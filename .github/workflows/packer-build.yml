name: "Packer checks"
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "images/**"
      - ".github/workflows/packer-build.yml"
      - "module/runners/templates/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  AWS_REGION: eu-west-1
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PACKER_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  # Validation does not build. The alias keeps fork PR checks independent of
  # repository configuration; release scripts accept only a 12-digit owner.
  PKR_VAR_source_ami_owner: ${{ vars.AMI_SOURCE_OWNER_ID || 'self' }}

jobs:
  verify_packer:
    name: Verify packer
    runs-on: ubuntu-latest
    container:
      image: index.docker.io/hashicorp/packer@sha256:12c441b8a3994e7df9f0e2692d9298f14c387e70bcc06139420977dbf80a137b # 1.11.2
    strategy:
      matrix:
        image: ["linux-al2023", "windows-core-2019", "windows-core-2022", "ubuntu-focal", "ubuntu-jammy", "ubuntu-jammy-arm64", "ubuntu-resolute", "ubuntu-resolute-arm64"]
    defaults:
      run:
        working-directory: images/${{ matrix.image }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: "Checkout"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: packer init
        run: packer init .
      - name: check packer formatting
        run: packer fmt -recursive -check=true .
      - name: packer validate
        run: packer validate -evaluate-datasources .
      - name: validate shared AMI variables
        if: startsWith(matrix.image, 'ubuntu-resolute')
        run: packer validate -evaluate-datasources -var-file=shared.pkrvars.hcl .
      - name: verify Chrome provisioning
        if: matrix.image == 'ubuntu-resolute'
        working-directory: ${{ github.workspace }}
        run: |
          apk add --no-cache jq
          bash images/chrome-install.test.sh
